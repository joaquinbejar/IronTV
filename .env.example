# Test credentials for manual API verification (curl). The app itself never
# reads this file — real credentials are entered in Settings and stored in
# the Keychain. Copy to .env and fill in; .env is gitignored.
HOST=http://host.example.com:8080
USERNAME=changeme
PASSWORD=changeme
